# Note
The conents within Public/ & Private/ are the source files within the UE5 project. 

Outside the Public & Private directory is the builtin source files such as 3D character (contents inside the Public/ & Private/ directories are where developer built sources exist)

The ML environment is included within the UE5 project environment. And can be seen being interacted with (such as the venv/ & outputted JSON file)